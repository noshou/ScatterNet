--- a/src/owl/stats/owl_stats_dist_exponpow.c
+++ b/src/owl/stats/owl_stats_dist_exponpow.c
@@ -33,14 +33,14 @@
     return a * x;
   }
   else if (b == 2) {
-      return std_gaussian_rvs (a / sqrt (2.0));
+      return (a / sqrt (2.0)) * std_gaussian_rvs ();
   }
   else {
     double x, h, u;
     double B = pow (1 / b, 1 / b);

     do {
-      x = std_gaussian_rvs (B);
+      x = B * std_gaussian_rvs ();
       u = sfmt_f64_3;
       h = -pow (fabs (x), b) + (x * x) / (2 * B * B) + (1 / b) - 0.5;
     }
